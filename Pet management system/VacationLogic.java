package OOP.oop_lab_2.problem5;
/*
public class VacationLogic {
    public static void leavePetWith(Person owner, Person caretaker) {
        if (owner.hasPet() && !(caretaker instanceof PhDStudent && owner.getPet() instanceof Dog)) {
            Animal pet = owner.getPet();
            pet.setOwner(owner);
            owner.removePet();
            caretaker.assignPet(pet);
            System.out.println(owner + " has left their pet " + pet.getName() + " with " + caretaker + ".");
        } else {
            System.out.println(owner + " cannot leave their pet with " + caretaker + ".");
        }
    }

    public static void retrievePet(Person owner, Person caretaker) {
        if (caretaker.hasPet()) {
            Animal pet = caretaker.getPet();
            if (pet.getOwner().equals(owner)) {
                caretaker.removePet();
                owner.assignPet(pet);
                System.out.println(owner + " has retrieved their pet " + pet.getName() + " from " + caretaker + ".");
            } else {
                System.out.println(owner + " cannot retrieve a pet that does not belong to them.");
            }
        } else {
            System.out.println(caretaker + " doesn't have any pet to return.");
        }
    }
}
*/
